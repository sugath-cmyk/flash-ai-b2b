{
  "name": "flash-ai-b2b",
  "version": "1.0.4",
  "private": true,
  "scripts": {
    "start": "cd backend && node dist/index.js",
    "build": "cd backend && rm -rf dist && npm install && npm run build"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
